Hello,

you need to click at the verify link to verify your email-address:
<f:uri.action pageUid="{subscribeVerifyUid}" extensionName="fpnewsletter" pluginName="pi1" controller="Log" action="verify" arguments="{uid: uid, hash: hash}" noCache="true" absolute="true" />

Best regards
{settings.company}